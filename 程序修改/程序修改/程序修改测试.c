#define _CRT_SECURE_NO_WARNINGS
#include <stdio.h>
#include <conio.h>
#include <string.h>
fun 
{
	int x; struct tt* y; 
}
*p;
struct tt a[4] = { 20,a + 1,15,a + 2,30,a + 3,17,a };
main()
{
	int i;
	p = a;
	for (i = 1; i <= 2; i++)
	{
		printf("%d\n", p->x); p = p->y;
	}
}